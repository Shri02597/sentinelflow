# Optional overrides. When unset:
#  - VITE_API_URL -> same-origin "/api" (Vercel rewrite proxies to the Render backend; local vite dev proxy handles it too)
#  - VITE_WS_URL  -> local dev: ws://localhost:8000, production build: wss://sentinelflow-api-g7p8.onrender.com
VITE_API_URL=
VITE_WS_URL=